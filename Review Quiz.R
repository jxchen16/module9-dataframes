#1 list can have name of the value with different data types while vector can only have one data type
#2 the signle braket notation returs piece of the information with same data type, as a filter, while the double brakets returns back a vector of the vector 
#3 you can use the braket with specific row and column like (name of the table)name[1,1]